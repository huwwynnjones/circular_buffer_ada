library project Circular_Buffer is

   for Languages use ("Ada");
   for Library_Name use "Circular_Buffer";
   for Source_Dirs use ("src");
   for Object_Dir use "obj";
   for Library_Dir use "lib";
   for Library_Kind use "static";

   package Compiler is
      for Switches ("ada") use ("-gnat12", "-gnatf", "-gnata", "-gnatw.ee");
   end Compiler;

   package Ide is
      for Vcs_Kind use "git";
   end Ide;

end Circular_Buffer;

